<%= error_messages_for(@category) %>

    <div class="form-input">
    <table summary="Category form fields">
      <tr>
        <th><%= f.label(:name) %></th>
        <td><%= f.text_field(:name) %></td>
      </tr>
      <tr>
        <th><%= f.label(:parent_id, "Parent Category") %></th>
        <td>
        	<%= f.select(:parent_id, @options) %>
        </td>
      </tr>
    </table>
    </div>