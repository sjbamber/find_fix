<div id="solution<%= solution.id %>" class="solution">
  <table><tr>
	<td>
     <%= show_voting(solution, post.id) %>
    </td>
    <td>
    <div class="show_post">
      <div class="post_display">
      
      	<p><%= solution.description.html_safe %></p>
	    <div class="post_footer">
	      <p class="highlight right">Submitted by <%= solution.user ? solution.user.username : 'Unknown' %> <%= time_ago_in_words(solution.updated_at.to_time) %> ago</p>
	      	<% if is_admin %>
	      	<table><tr>
	          <td><%= link_to("Edit this solution", {:action => 'edit', :id => solution.id}, :class => 'solution_edit') %></td>
	          <td><%= link_to("Delete this solution", {:action => 'delete', :id => solution.id}, :class => 'solution_delete')%></td>
	      	</tr></table>
	      	<% end %>
	    </div>
      </div>
      
    </div>
    <div class="comments comments_<%=solution.class%><%=solution.id%>">
    	<% unless solution.comments.size == 0 %> 
	    	<strong>Comments:</strong>	
	    	<%= render solution.comments, :post_id => solution.id   %>
	    <% end %>
    </div>
    <%= show_comment_form(solution, post.id) %>
   </td>
   </tr></table>
   <br /><hr /><br />
</div>